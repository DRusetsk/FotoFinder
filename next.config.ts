import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images:{
    domains:["lassonde.yorku.ca", "live.staticflickr.com", "attractionsontario.ca", "firebasestorage.googleapis.com"]
  }
};

export default nextConfig;